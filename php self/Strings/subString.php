<?php
	$quote = "only those who will risk going too far can possibly find out how far one can go.";
	//echo substr($quote, 2);
		echo substr($quote, 2, -8);
		
		
		//string substr(string $string, int $start[, int $length])

?>